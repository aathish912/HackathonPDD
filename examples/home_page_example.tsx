/*\n * examples/home_page_example.tsx\n *\n * This example demonstrates the intended way to use the exported default component\n * from the module named \"page\" (located at app/page.tsx).\n *\n * How it works:\n * - <HomePage /> collects an email, validates it, stores it in localStorage under\n *   the key \"userEmail\", then navigates to /task via Next.js App Router.\n * - A typical next step is a /task page that reads \"userEmail\" from localStorage\n *   to prefill or associate tasks with the user.\n *\n * Usage:\n * 1) Create app/task/page.tsx (see TaskPageExample below).\n * 2) Run the Next.js app and visit /.\n */\n\n'use client'\n\nimport * as React from 'react'\n\n// Import the default export from the module named \"page\" (app/page.tsx).\n// Path is relative to this file (examples/ -> app/).\nimport HomePage from '../app/page'\n\n/**\n * ExampleUsagePage\n *\n * Input parameters:\n * - None.\n *\n * Output:\n * - Renders the HomePage component, which:\n *   - Accepts user email input\n *   - Validates it client-side\n *   - Writes localStorage[\"userEmail\"] = normalizedEmail\n *   - Redirects to /task using Next.js useRouter()\n */\nexport default function ExampleUsagePage() {\n  return <HomePage />\n}\n\n/**\n * TaskPageExample\n *\n * This is an example implementation for app/task/page.tsx.\n *\n * Input parameters:\n * - None.\n *\n * Output:\n * - Reads localStorage[\"userEmail\"] that was set by <HomePage />\n * - Displays it (or shows a fallback message if missing)\n *\n * Note:\n * - localStorage is only available on the client, so this must be a client component.\n */\nexport function TaskPageExample() {\n  const [email, setEmail] = React.useState<string | null>(null)\n\n  React.useEffect(() => {\n    setEmail(localStorage.getItem('userEmail'))\n  }, [])\n\n  return (\n    <main style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>\n      <h1>Task Page</h1>\n      <p>\n        Email from HomePage: <strong>{email ?? '(not set)'}</strong>\n      </p>\n      <p>Implement your task entry UI here.</p>\n    </main>\n  )\n}\n